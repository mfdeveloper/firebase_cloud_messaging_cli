# Pytest configuration for FCM Notification Sender tests
[pytest]

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts = -v --tb=short

# Coverage options (when running with pytest-cov)
# Run with: pytest --cov=. --cov-report=html
