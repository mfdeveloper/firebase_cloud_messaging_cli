# =============================================================================
# FCM Notification Sender - Dependencies (Legacy)
# =============================================================================
# This project uses Poetry for dependency management.
#
# Recommended: Use Poetry instead
#   poetry install          # Install all dependencies
#   poetry install --with dev  # Include dev dependencies
#
# For pip users, this file is auto-generated from pyproject.toml:
#   poetry export -f requirements.txt --output requirements.txt
#
# Install with pip:
#   pip install -r requirements.txt
# =============================================================================

# Core dependencies
firebase-admin>=6.0.0

# Dev dependencies (install with: pip install -r requirements-dev.txt)
# Or use: poetry install --with dev
