# =============================================================================
# FCM Notification Sender - Dependencies
# =============================================================================
# This file references pyproject.toml as the single source of truth.
#
# Install with: pip install -r requirements.txt
# Or directly:  pip install -e ".[dev]"
# =============================================================================

# Install the package in editable mode with all dev dependencies
-e ".[dev]"
